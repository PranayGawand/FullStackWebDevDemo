The index.html file is the one to start our project.

The doc folder contains:
	CMPUT391_Group_1_Project_Report.doc
	userDocumentation.doc

The modle folder contains:
	2 basic .sql files. One is using for setup, and another one is for demo.

The View folder contains:
  All of the .html files we use exept the index.html because index.html is 
		required to be put in the proj1 folder.

The controller folder contains:
	All of the .jsp files we used exept the search.jsp because we will use the 
		files in the folder "WEB-INF/class"

The js folder contains:
	All of the .js filse using for testing whether the input is good or not




